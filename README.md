# Bourgie
Time is money. Save both

To run:

```# Install node and bower
brew install node
npm install -g bower```

```# download dependencies and run
npm install
cd public
bower install```

Or, run using `./run.sh`